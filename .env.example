# PKI Certificate Generation Configuration
# Copy this file to .env and update with your values

# Directory for certificate output
CERT_DIR=./k8s-pki

# Master node IP address
MASTER_IP=10.48.4.181

# Master node hostname (must match the actual server hostname)
HOSTNAME=k3s-dev

# Kubernetes cluster name
CLUSTER_NAME=kubernetes

# Service network CIDR
SERVICE_CIDR=10.43.0.0/16

# Cluster DNS IP (first usable IP in SERVICE_CIDR)
CLUSTER_DNS=10.43.0.10

# Path to your agenix identity key
KEY_PATH=/home/batonac/.ssh/id_agenix

# Fully qualified domain name for the cluster
FQDN=k3s-dev.batonac.com